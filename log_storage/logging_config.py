import logging

logger_debug = logging.getLogger('debug').debug
logger_info = logging.getLogger('info').info
logger_error = logging.getLogger('error').error
logger_develop = logging.getLogger('development').debug
